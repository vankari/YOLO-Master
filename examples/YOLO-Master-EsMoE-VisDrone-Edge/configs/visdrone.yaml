# VisDrone2019-DET dataset — vertical-domain config for YOLO-Master edge task.
# 10 classes, lots of small objects (UAV perspective). Used to fine-tune EsMoE-N.
# Relative path (matches upstream VisDrone.yaml convention) so the example is portable;
# downloads/labels land under <cwd>/datasets/VisDrone regardless of the machine.

path: ../datasets/VisDrone # dataset root dir (relative to the YOLO-Master repo root)
train: images/train # 6471 images
val: images/val     # 548 images
test: images/test   # 1610 images

names:
  0: pedestrian
  1: people
  2: bicycle
  3: car
  4: van
  5: truck
  6: tricycle
  7: awning-tricycle
  8: bus
  9: motor

# Auto-download + VisDrone→YOLO label conversion (same logic as upstream VisDrone.yaml).
download: |
  import shutil
  from pathlib import Path
  from PIL import Image

  from ultralytics.utils import ASSETS_URL, TQDM
  from ultralytics.utils.downloads import download


  def visdrone2yolo(dir, split, source_name=None):
      source_dir = dir / (source_name or f"VisDrone2019-DET-{split}")
      images_dir = dir / "images" / split
      labels_dir = dir / "labels" / split
      labels_dir.mkdir(parents=True, exist_ok=True)
      if (source_images_dir := source_dir / "images").exists():
          images_dir.mkdir(parents=True, exist_ok=True)
          for img in source_images_dir.glob("*.jpg"):
              img.rename(images_dir / img.name)
      for f in TQDM((source_dir / "annotations").glob("*.txt"), desc=f"Converting {split}"):
          img_size = Image.open(images_dir / f.with_suffix(".jpg").name).size
          dw, dh = 1.0 / img_size[0], 1.0 / img_size[1]
          lines = []
          with open(f, encoding="utf-8") as file:
              for row in [x.split(",") for x in file.read().strip().splitlines()]:
                  if row[4] != "0":  # skip ignored regions
                      x, y, w, h = map(int, row[:4])
                      cls = int(row[5]) - 1
                      x_center, y_center = (x + w / 2) * dw, (y + h / 2) * dh
                      w_norm, h_norm = w * dw, h * dh
                      lines.append(f"{cls} {x_center:.6f} {y_center:.6f} {w_norm:.6f} {h_norm:.6f}\n")
          (labels_dir / f.name).write_text("".join(lines), encoding="utf-8")


  dir = Path(yaml["path"])
  urls = [
      f"{ASSETS_URL}/VisDrone2019-DET-train.zip",
      f"{ASSETS_URL}/VisDrone2019-DET-val.zip",
      f"{ASSETS_URL}/VisDrone2019-DET-test-dev.zip",
  ]
  download(urls, dir=dir, threads=4)
  splits = {"VisDrone2019-DET-train": "train", "VisDrone2019-DET-val": "val", "VisDrone2019-DET-test-dev": "test"}
  for folder, split in splits.items():
      visdrone2yolo(dir, split, folder)
      shutil.rmtree(dir / folder)
