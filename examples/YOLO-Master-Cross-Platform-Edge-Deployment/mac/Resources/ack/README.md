# Acknowledgement logos (About page)

Drop the upstream project logos here; make_app.sh bundles this folder into the app
and Info.swift loads them (falling back to a lettered placeholder if absent):

- `tencent.png`      — Tencent / YOLO-Master
- `ultralytics.png`  — Ultralytics
- `apple.jpeg`       — Apple Core ML / coremltools
