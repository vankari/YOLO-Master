# Model Zoo submission

- [ ] Add exactly one `manifest.
- [ ] Do not commit weights or binaries.
- [ ] Use an allowlisted HTTPS ONNX URL with SHA-256 and size.
- [ ] Run `python tools/model_zoo_automation.py validate <manifest>`.
- [ ] A maintainer must apply `model-zoo-benchmark`.
